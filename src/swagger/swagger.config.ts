import { SwaggerConfig } from './swagger.interface';

export const SWAGGER_CONFIG: SwaggerConfig = {
  title: 'User Management API',
  description: 'This is an file API write on Swagger',
  version: '1.0',
  tags: ['User'],
};
